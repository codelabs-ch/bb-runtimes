project hello is
   for Main use ("hello.adb");
   package Compiler is
      for Switches ("Ada") use ("-g");
   end Compiler;
   package Binder is
      for Switches ("Ada") use ("-E");
   end Binder;
end hello;
